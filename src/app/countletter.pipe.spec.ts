import { CountletterPipe } from './countletter.pipe';

describe('CountletterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountletterPipe();
    expect(pipe).toBeTruthy();
  });
});
